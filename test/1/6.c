#if 0
AVL 트리는 일반 Tree 가 가지고 있는 약점을 보완하기 위해 고안되었다.
Tree 에 데이터가 1, 2, 3, 4, 5, 6, 7 과 같이
한쪽 방향으로 편중되어 들어오면 결국 이 구조는 Queue 형태가 된다.
이와 같은 불상사를 방지하기 위해 고안된 것이 AVL 트리에 해당한다.
균형이 깨지면(레벨 차이 2) 회전하여 이진 트리 형태를 잡아줌
AVL 트리의 약점이라면 회전이 너무 빈번하여
데이터의 삽입과 삭제가 많은 구조에서는 별로 좋지 못하다는 것이다.
#endif
