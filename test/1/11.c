#if 0
Intel Architecture 의 경우 CISC 구조로 명령어의 길이가 가변적이다.
또한 Memory 2 Memory 연산이 가능하며
반도체 다이 사이즈가 크기 때문에
복잡한 분기 예측기와 같은 여러가지 기능 유닛들을 넣을 수 있다.
ARM Architecture 의 경우 RISC 구조로 명령어의 길이가 4 byte 로 일정하다.
또한 Memory 2 Memory 연산이 불가능하기 때문에
ldr/str 라는 별도의 명령어를 가지고 있고
이 때문에 Load/Store Architecture 라고 불린다.
또한 반도체 다이 사이즈가 작기 때문에
최소한의 필요한 기능 유닛들만 배치한다.
물론 Intel 대비 그렇다는 것이고 요즘은 ARM 도 많이 복잡해짐
#endif
